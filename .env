# Local environment variables for docker-compose
# Copy values from .env.example or replace the placeholders below.

# Azure SQL connection string (do NOT commit real secrets)
ConnectionStrings__Default=Server=tcp:fffff.database.windows.net,1433;Database=dbstms;User ID=stms_app;Password=kawan@321;Encrypt=True;TrustServerCertificate=False;Connection Timeout=30;

# JWT secret used by backend
JWT__Secret=replace-with-a-long-random-secret

# ASP.NET Core environment
ASPNETCORE_ENVIRONMENT=Development

# Frontend runtime API base (browser should reach this host:port)
API_BASE=http://localhost:5001

